/**
 * Alias NotesManager → NoteManager (contrat Sous-Prompt 10)
 * Le CRUD + recherche full-text est implémenté dans NoteManager.
 */
export { NoteManager as NotesManager, NoteManager } from './NoteManager';
export { default } from './NoteManager';
